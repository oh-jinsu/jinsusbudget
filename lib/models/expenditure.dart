class ExpenditureModel {
  final int id;
  final String label;
  final int amount;
  final DateTime timestamp;

  ExpenditureModel({
    required this.id,
    required this.label,
    required this.amount,
    required this.timestamp,
  });
}
